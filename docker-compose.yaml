services:
  lossyloop:
    container_name: lossyloop-go
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /home/src
    volumes:
      - ./output:/home/src/output
    networks:
      - default
    environment:
      - TZ=Asia/Tokyo
      - LANG=C.UTF-8